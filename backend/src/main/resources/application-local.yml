spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/shogunate
    username: shogunate
    password: shogunate
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080

jwt:
  secret: local-dev-secret-change-in-production-min-32-chars
  expiration-ms: 86400000
