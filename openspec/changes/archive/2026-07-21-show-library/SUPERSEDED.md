# Superseded

This change was superseded by `shared-show-catalog` before implementation.

**Do not apply `show-library`.** The implemented design lives in:

`openspec/changes/archive/2026-07-20-shared-show-catalog/`

Reason: data model pivoted from per-user show snapshots to a shared global catalog with per-user `user_library` membership.
